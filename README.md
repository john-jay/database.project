# database.project

This contains the full structure of the AdventureWorks database, but no data.

To import a database into a VS database project, in SQL Server Object Explorer > right-click database > Create new project.
